# Agent Instructions

This workspace contains a simple Node.js JavaScript project for an AI course lab assignment.

- The main file is `index.js`, which currently has a placeholder comment about implementing a function to calculate the sum of two numbers.
- Run JavaScript files directly using `node <filename>.js`.
- Follow standard JavaScript coding conventions: use `const` and `let` for variables, descriptive function and variable names, and proper indentation.
- If dependencies are needed (e.g., for AI libraries like TensorFlow.js), initialize with `npm init` and add them via `npm install`.
- No build or test commands are configured yet; add them to `package.json` scripts as the project grows.
- Potential pitfalls: Ensure code handles edge cases (e.g., non-numeric inputs for sum function) and add error handling.
